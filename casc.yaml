groovy:
  - script: >
      import com.cloudbees.hudson.plugins.folder.Folder;
      import jenkins.model.Jenkins;
      
      def numOfTeams = 20;
      (1..numOfTeams).each { currentIndex ->
        Jenkins.instance.createProject(Folder.class, 'Team_'+ currentIndex);
      }
